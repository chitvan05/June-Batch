import { InrToUsedPipe } from './inr-to-used.pipe';

describe('InrToUsedPipe', () => {
  it('create an instance', () => {
    const pipe = new InrToUsedPipe();
    expect(pipe).toBeTruthy();
  });
});
